services:
  - type: web
    name: ai_app_backend
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
